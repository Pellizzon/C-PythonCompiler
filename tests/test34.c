{
    in = readln();
    x = (1 + in) * 2;
    println(x);
    i = 0;
    while (i < 10)
    {
        println(i);
        i = i + 1;
    }
    while (i > 0)
    {
        println(i);
        i = i - 1;
    }
}